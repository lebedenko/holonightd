# Ecosystem Maintainability Standardization — holonightd

| ID | Task | State | Verification |
|---|---|---|---|
| HOLONIGHTD-01 | Remove `install:local`; retain build/test tooling and add non-mutating staged package verification. | Planned | — |
| HOLONIGHTD-02 | Keep service enablement/start and administrator configuration outside CMake and install scripts. | Planned | — |
| HOLONIGHTD-03 | Audit service-unit paths, executable references, sample configuration ownership, and optional libsystemd behavior. | Planned | — |
| HOLONIGHTD-04 | Require tests, format-check, tidy-src, install-tree checks, and release metadata checks; reference Shared CI Build Infrastructure for image work. | Planned | — |

Allowed states are `Planned`, `Ready`, `In Progress`, `Done`, `Blocked`, and `Superseded`. Record exact commands
and results before marking a task `Done`.
